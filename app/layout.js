export const metadata = {
  title: "Choice Bagging Equipment",
  description: "CBE Customer Portal & Parts Store",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0, padding: 0 }}>{children}</body>
    </html>
  );
}
